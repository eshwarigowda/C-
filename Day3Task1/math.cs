using System;
public class Mathop
{
    public static void Run()
    {
        int c = 10;
        int d = 30;
        Math.Min(c, d);
        Math.Max(c, d);
        Math.Sqrt(c);
        Math.Sqrt(d);
        Math.Abs(c);
        Math.Round((double)d);

    }
}